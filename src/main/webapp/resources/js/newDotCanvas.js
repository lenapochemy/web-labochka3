canvasDrawer.drawArea(canvasDrawer.lastR);

// достат значение радиуса и перерисовывает график
// новые точки чтобы добавить
//
// r = sessionStorage.getItem("lastR");
// if(r == null) r = 1;
// canvasDrawer.drawArea(r);
//
// var canvas = document.getElementById("canvas");
// var cd = new CanvasDrawer(canvas);
// cd.lastR = sessionStorage.getItem("lastR");
// cd.drawArea(cd.lastR);
